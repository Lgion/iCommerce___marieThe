// Calendar Model
// Modèle pour la gestion des événements/calendrier

/**
 * Calendar
 * @typedef {Object} Calendar
 * @property {string} ownerId - ID du propriétaire
 * @property {Array<Object>} events - Liste des événements
 */

const Calendar = {
  ownerId: '',
  events: [],
};

module.exports = Calendar;
